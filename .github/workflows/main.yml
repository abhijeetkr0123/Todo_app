name: Todo App CI

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Print confirmation
        run: echo "🚀 Workflow triggered for $GITHUB_REPOSITORY"
